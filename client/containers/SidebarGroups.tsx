import React from 'react'
import { Paper } from '@material-ui/core';

const SidebarGroups = () => (
  <Paper
    square
    elevation={20}
    classes={{
      root: 'sidebar-groups-root',
    }}>
  </Paper>
)

export default SidebarGroups;